import './style.css'; 

import box from './components/Box';

const app = document.getElementById('app');

box(app, 300, 60, 59);

box(app, 100, 400, 800);
box(app, 145, 500, 200);
box(app, 134, 400, 500);
