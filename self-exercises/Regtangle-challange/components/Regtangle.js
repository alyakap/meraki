class Regtangle {
    constructor(holder, width, height){
        this.holder=holder;
        this.width=width;
        this.height=height;
    }
}

export default (holder, width, height)= new Regtangle(holder, width, height)